runtime_indexes.json (repo root) = index DEFINITIONS (schema, sources, build rules).
runtime/out/ = COMPUTED index MAPS (materialized outputs).

runtime/out/runtime_indexes_computed_v1.json is a legacy computed snapshot retained for reference.
