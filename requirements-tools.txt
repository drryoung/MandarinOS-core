# Optional tooling for MandarinOS repo scripts (pip install -r requirements-tools.txt)
# Used by tools/enrich_characters_1200_pinyin_gloss.py for pinyin backfill.
pypinyin>=0.50.0
# ui_server /api/gloss — machine English for transcript lines when cards omit text_en (pip install deep-translator)
deep-translator>=1.11.4
